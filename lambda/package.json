{
  "name": "image-upload-lambda",
  "version": "1.0.0",
  "description": "Lambda function for handling image uploads to S3",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "aws",
    "lambda",
    "s3",
    "dynamodb",
    "image-upload"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.x.x",
    "@aws-sdk/client-dynamodb": "^3.x.x",
    "@aws-sdk/s3-request-presigner": "^3.x.x",
    "uuid": "^9.x.x"
  }
}
